using NueGames.NueDeck.Scripts.Enums;
using NueGames.NueDeck.Scripts.Managers;
using UnityEngine;

//namespace NueGames.NueDeck.Scripts.Card.CardActions
//{
  //  public class EndTurn : CardActionBase
    //{
     //   public override CardActionType ActionType => CardActionType.EndTurn;
//        public override void DoAction(CardActionParameters actionParameters)
     //   {
     //      // Optional: small feedback
     //       if (FxManager != null)
      //          FxManager.SpawnStaticText(actionParameters.SelfCharacter.transform, "End Turn", 0, 1);

     //       if (AudioManager != null)
      //          AudioManager.PlayOneShot(AudioActionType.NoLight); // placeholder if you want a sound; replace with a proper AudioActionType

            // End the player's turn immediately
     //       CombatManager.EndTurn();
    //    }
   //// }
//}
